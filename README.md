# Laboratory Work 9 — Useful Third-Party Components for React

This folder contains three **separate** React apps as required:
1. `aggrid-app` — AG Grid over GitHub API (sorting, filtering, pagination).
2. `shoppinglist` — Shopping List with MUI (layout, modal dialog, list).
3. `routerapp` — Client-side routing (React Router v6).

For each app:
```bash
npm install
npm start
```

Branching & Video: please commit each task in a separate branch and record the demonstration as required by the lab brief
